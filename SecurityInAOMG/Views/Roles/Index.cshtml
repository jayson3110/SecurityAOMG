@model IEnumerable<SecurityInAOMG.Models.UseGroup>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_View.cshtml";
}

<h2>Roles in each Group</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Group_user_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Roles1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Roles2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Roles3)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Group_user_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Group_user_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Roles1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Roles2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Roles3)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Group_user_Id }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
